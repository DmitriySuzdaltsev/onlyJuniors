{
  "name": "ui",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "body-parser": "^1.19.0",
    "mongoose": "^5.12.8"
  },
  "scripts": {
    "build": "npm --prefix ./ui install  && npm --prefix ./ui run build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "engines": {
    "node": "12.16.2"
  },
  "devDependencies": {
    "prettier": "2.3.0"
  }
}
